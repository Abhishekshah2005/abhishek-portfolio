/**
 * Curated Lucide set — the only Lucide icons sanctioned for the UI.
 *
 * Re-exported with domain names so components import intent, not library
 * specifics, and swapping the underlying icon never touches call sites. Tree
 * shakeable: only what you import ships.
 */
export {
  ArrowDown as IconArrowDown,
  ArrowUp as IconArrowUp,
  ArrowUpRight as IconArrowUpRight,
  ChevronRight as IconChevronRight,
  ChevronDown as IconChevronDown,
  X as IconClose,
  Menu as IconMenu,
  GripVertical as IconDrag,
  Scan as IconInspect,
  ExternalLink as IconExternal,
  Mail as IconMail,
  Github as IconGithub,
  Linkedin as IconLinkedin,
  Play as IconPlay,
  Pause as IconPause,
  Volume2 as IconSoundOn,
  VolumeX as IconSoundOff,
  Trophy as IconTrophy,
  Lock as IconLock,
  Check as IconCheck,
  Circle as IconNode,
  Sparkles as IconSpark,
  Loader as IconLoader,
} from 'lucide-react';

export type { LucideIcon } from 'lucide-react';
